block('carousel')(
    // content()(function() {
    //     return {
    //         elem: 'inner',
    //         content: applyNext()
    //     };
    // }),
    elem('paginator').mix()({ block: 'clearfix' })
);
